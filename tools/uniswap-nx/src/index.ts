// Local stub for the @universe/uniswap-nx workspace, which is not included in the
// seed checkout. Provides just enough for `bun install` and root `tsc -b` to resolve.
// The real package houses Nx generators (e.g. `nx generate @universe/uniswap-nx:package`).
export {}
